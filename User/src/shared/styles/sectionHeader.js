export const sectionHeader = {
  // Main Section Header Wrapper
  wrapper:
    "mb-4 sm:mb-5 flex flex-col gap-2 sm:gap-3 border-b border-[var(--color-border)] pb-3 lg:flex-row lg:items-end lg:justify-between transition-colors duration-200",

  // Badge
  badge:
    "inline-flex items-center gap-1.5 rounded-full bg-[var(--color-primary)]/10 px-3 py-1 text-xs font-bold text-[var(--color-primary)] shrink-0 w-fit leading-none",

  // Content
  content: "flex-1 min-w-0",

  // Title
  title:
    "mt-1 text-2xl sm:text-3xl lg:text-4xl font-extrabold text-[var(--color-text-primary)] tracking-tight leading-tight",

  // Subtitle
  subtitle:
    "text-[var(--color-primary)] font-semibold inline-block",

  // Description
  description:
    "mt-1 max-w-2xl text-sm sm:text-base text-[var(--color-text-secondary)] leading-relaxed",

  // Actions
  actions:
    "flex items-center gap-2 shrink-0 pt-1 lg:pt-0 w-full sm:w-auto",
};