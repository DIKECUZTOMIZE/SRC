export const iconSizeTokens = {
    // Pixel values for Lucide/Icon components that accept `size` props (e.g. <Icon size={16} />)
    pixels: {
        xs: 12,
        sm: 14,
        md: 16,
        lg: 20,
        xl: 24,
        '2xl': 32,
    },

    // Tailwind CSS dimension utility classes (width + height + flex alignment)
    classes: {
        xs: "w-3 h-3 shrink-0",
        sm: "w-3.5 h-3.5 shrink-0",
        md: "w-4 h-4 shrink-0",
        lg: "w-5 h-5 shrink-0",
        xl: "w-6 h-6 shrink-0",
        '2xl': "w-8 h-8 shrink-0",
    },
};